import { Injectable } from '@nestjs/common';import jwt from'jsonwebtoken';import{RuntimeConfigService}from'@elhafez/config';import type{TokenCodec,TokenPayload}from'../application/auth.repository';@Injectable()export class JwtTokenCodec implements TokenCodec{readonly accessTtl:number;readonly refreshTtl:number;constructor(private readonly config:RuntimeConfigService){this.accessTtl=config.get('ACCESS_TOKEN_TTL_SECONDS');this.refreshTtl=config.get('REFRESH_TOKEN_TTL_SECONDS');}signAccess(payload:TokenPayload):string{return jwt.sign(payload,this.config.get('JWT_ACCESS_SECRET'),{algorithm:'HS256',expiresIn:this.accessTtl});}signRefresh(payload:TokenPayload):string{return jwt.sign(payload,this.config.get('JWT_REFRESH_SECRET'),{algorithm:'HS256',expiresIn:this.refreshTtl});}verifyAccess(token:string):TokenPayload{const p=jwt.verify(token,this.config.get('JWT_ACCESS_SECRET'),{algorithms:['HS256']})as TokenPayload;if(p.typ!=='access')throw new Error('Wrong token type');return p;}verifyRefresh(token:string):TokenPayload{const p=jwt.verify(token,this.config.get('JWT_REFRESH_SECRET'),{algorithms:['HS256']})as TokenPayload;if(p.typ!=='refresh')throw new Error('Wrong token type');return p;}}
