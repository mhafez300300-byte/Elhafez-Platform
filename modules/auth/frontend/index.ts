import type{AuthTokens}from'../contracts';export const createAuthClient=(baseUrl:string)=>({async login(login:string,password:string):Promise<AuthTokens>{const r=await fetch(`${baseUrl}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({login,password})});if(!r.ok)throw new Error('Login failed');return r.json()as Promise<AuthTokens>;},async refresh(refreshToken:string):Promise<AuthTokens>{const r=await fetch(`${baseUrl}/auth/refresh`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({refreshToken})});if(!r.ok)throw new Error('Refresh failed');return r.json()as Promise<AuthTokens>;}});
