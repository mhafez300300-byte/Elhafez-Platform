export interface AuthTokens { accessToken: string; refreshToken: string; accessExpiresInSeconds: number; refreshExpiresInSeconds: number; }
export interface CredentialView { userId: string; login: string; createdAt: string; updatedAt: string; }
