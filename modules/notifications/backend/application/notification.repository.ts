import type{NotificationChannel,NotificationView}from'../../contracts';export interface NotificationRepository{create(input:{recipientUserId:string;channel:NotificationChannel;subject:string;body:string}):Promise<NotificationView>;markSent(id:string):Promise<NotificationView>;markFailed(id:string,error:string):Promise<NotificationView>;listForUser(userId:string,limit:number):Promise<NotificationView[]>;}export const NOTIFICATION_REPOSITORY=Symbol('NOTIFICATION_REPOSITORY');export interface NotificationProvider{channel:NotificationChannel;send(input:{recipientUserId:string;subject:string;body:string}):Promise<void>;}export const IN_APP_NOTIFICATION_PROVIDER=Symbol('IN_APP_NOTIFICATION_PROVIDER');
